export function Canvas() {
  const canvas = document.createElement('canvas');
  canvas.width = 1280;
  canvas.height = 720;
  canvas.style.border = '2px solid #ececec';
  canvas.style.background = '#fff';

  // Placeholder for drawing and effects logic

  return canvas;
}